<%= form_for(@filename) do |f| %>
  <% if @filename.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@filename.errors.count, "error") %> prohibited this filename from being saved:</h2>

      <ul>
      <% @filename.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :update_type %><br />
    <%= f.text_field :update_type %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
